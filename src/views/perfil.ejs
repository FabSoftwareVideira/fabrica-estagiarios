<!DOCTYPE html>
<html lang="pt-BR">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Meu Perfil</title>
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>

    <%- include('partials/header') %>

        <main class="container">
            <%- include('partials/flash') %>

                <div class="login-box">
                    <h1>Meu Perfil</h1>
                    <form action="/perfil" method="POST">
                        <label for="nome">Nome completo</label>
                        <input type="text" id="nome" name="nome" maxlength="150" value="<%= usuario.nome %>" required>

                        <label for="email">E-mail</label>
                        <input type="email" id="email" value="<%= usuario.email %>" readonly>
                        <p class="text-muted">O e-mail não pode ser alterado porque identifica sua conta Google ou
                            GitHub.</p>

                        <% if (usuario.tipo==='aluno' ) { %>
                            <label for="matricula">Matrícula <span class="text-muted">(opcional)</span></label>
                            <input type="text" id="matricula" name="matricula" maxlength="30"
                                value="<%= aluno ? (aluno.matricula || '') : '' %>">
                            <% } %>

                                <div class="button-group">
                                    <button type="submit" class="btn-success">Salvar alterações</button>
                                    <a href="<%= usuario.tipo === 'professor' ? '/professor/dashboard' : '/aluno/dashboard' %>"
                                        class="btn btn-secondary">Cancelar</a>
                                </div>
                    </form>
                </div>
        </main>

        <%- include('partials/footer') %>
</body>

</html>